以下是基于 **PySide6** 的20个逐步深入的Demo示例，涵盖从基础到高级的核心知识点。每个Demo均结合实际场景设计，并附学习目标与参考资源。建议按顺序实践，逐步掌握GUI开发技能。

---

### **一、基础篇（Demo 1-8）**
#### **Demo 1：创建第一个窗口**
**目标**：掌握窗口创建与事件循环  
**代码**：  
```python
import sys
from PySide6.QtWidgets import QApplication, QWidget

app = QApplication(sys.argv)
window = QWidget()
window.setWindowTitle("Hello PySide6")
window.resize(400, 300)
window.show()
sys.exit(app.exec())
```
**学习点**：`QApplication`、`QWidget`、事件循环机制。

#### **Demo 2：按钮与信号槽**
**目标**：理解信号与槽机制  
**代码**：  
```python
from PySide6.QtWidgets import QPushButton, QMessageBox

button = QPushButton("点击我", window)
button.clicked.connect(lambda: QMessageBox.information(window, "提示", "按钮被点击！"))
```
**学习点**：`QPushButton`、`clicked`信号、匿名函数槽。

#### **Demo 3：布局管理器（垂直/水平布局）**
**目标**：使用`QVBoxLayout`和`QHBoxLayout`  
**代码**：  
```python
from PySide6.QtWidgets import QVBoxLayout, QLabel

layout = QVBoxLayout()
layout.addWidget(QLabel("标签1"))
layout.addWidget(QPushButton("按钮"))
window.setLayout(layout)
```
**学习点**：布局管理器实现控件自动排列。

#### **Demo 4：输入框与标签交互**
**目标**：处理用户输入  
**代码**：  
```python
from PySide6.QtWidgets import QLineEdit

line_edit = QLineEdit()
line_edit.textChanged.connect(lambda text: label.setText(f"输入内容：{text}"))
```
**学习点**：`QLineEdit`、`textChanged`信号。

#### **Demo 5：单选按钮与复选框**
**目标**：处理多选项逻辑  
**代码**：  
```python
from PySide6.QtWidgets import QRadioButton, QCheckBox

radio = QRadioButton("选项1")
checkbox = QCheckBox("同意协议")
checkbox.stateChanged.connect(lambda: print("状态变更"))
```
**学习点**：`QRadioButton`、`QCheckBox`状态监听。

#### **Demo 6：列表控件（QListWidget）**
**目标**：动态管理列表项  
**代码**：  
```python
from PySide6.QtWidgets import QListWidget

list_widget = QListWidget()
list_widget.addItems(["Item1", "Item2"])
list_widget.itemClicked.connect(lambda item: print(item.text()))
```
**学习点**：列表项的增删与事件绑定。

#### **Demo 7：消息对话框**
**目标**：弹窗交互设计  
**代码**：  
```python
from PySide6.QtWidgets import QMessageBox

QMessageBox.question(window, "确认", "确定要退出？", QMessageBox.Yes | QMessageBox.No)
```
**学习点**：`QMessageBox`的多种类型（信息、警告、提问）。

#### **Demo 8：文件选择对话框**
**目标**：文件读写操作  
**代码**：  
```python
from PySide6.QtWidgets import QFileDialog

file_path, _ = QFileDialog.getOpenFileName(window, "选择文件")
```
**学习点**：`QFileDialog`、文件路径处理。

---

### **二、进阶篇（Demo 9-15）**
#### **Demo 9：主窗口框架（QMainWindow）**
**目标**：菜单栏、工具栏、状态栏  
**代码**：  
```python
from PySide6.QtWidgets import QMainWindow, QAction

main_window = QMainWindow()
menu = main_window.menuBar().addMenu("文件")
action = QAction("打开", main_window)
menu.addAction(action)
```
**学习点**：`QMainWindow`结构设计。

#### **Demo 10：多标签界面（QTabWidget）**
**目标**：多页面切换  
**代码**：  
```python
from PySide6.QtWidgets import QTabWidget

tab_widget = QTabWidget()
tab_widget.addTab(QWidget(), "标签1")
tab_widget.addTab(QWidget(), "标签2")
```
**学习点**：多标签管理。

#### **Demo 11：表格数据展示（QTableView）**
**目标**：模型-视图架构  
**代码**：  
```python
from PySide6.QtCore import QAbstractTableModel
from PySide6.QtWidgets import QTableView

class TableModel(QAbstractTableModel):
    # 自定义数据模型...
table = QTableView()
table.setModel(TableModel())
```
**学习点**：自定义模型与视图绑定。

#### **Demo 12：数据库连接（SQLite）**
**目标**：GUI与数据库交互  
**代码**：  
```python
import sqlite3
conn = sqlite3.connect("data.db")
cursor = conn.cursor()
cursor.execute("CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, name TEXT)")
```
**学习点**：`sqlite3`集成与CRUD操作。

#### **Demo 13：样式表美化（QSS）**
**目标**：自定义控件外观  
**代码**：  
```python
button.setStyleSheet("""
    QPushButton { background: #4CAF50; border-radius: 5px; }
    QPushButton:hover { background: #45a049; }
""")
```
**学习点**：CSS语法与样式继承。

#### **Demo 14：事件过滤器**
**目标**：全局事件拦截  
**代码**：  
```python
class MyWidget(QWidget):
    def eventFilter(self, obj, event):
        if event.type() == QtCore.QEvent.MouseButtonPress:
            print("鼠标按下事件")
        return super().eventFilter(obj, event)
```
**学习点**：事件监听与过滤机制。

#### **Demo 15：多线程任务**
**目标**：防止界面卡顿  
**代码**：  
```python
from PySide6.QtCore import QThread, Signal

class Worker(QThread):
    progress = Signal(int)
    def run(self):
        for i in range(100):
            self.progress.emit(i)
```
**学习点**：`QThread`与信号通信。

---

### **三、高级篇（Demo 16-20）**
#### **Demo 16：图表绘制（PyQtGraph）**
**目标**：动态数据可视化  
**代码**：  
```python
import pyqtgraph as pg
plot_widget = pg.PlotWidget()
plot_widget.plot([1,2,3], [4,5,6])
```
**学习点**：集成第三方图表库。

#### **Demo 17：3D图形（Qt3D）**
**目标**：基础3D渲染  
**代码**：  
```python
from PySide6.Qt3DExtras import Qt3DWindow
view = Qt3DWindow()
# 配置3D场景...
```
**学习点**：`Qt3D`模块使用。

#### **Demo 18：国际化（多语言支持）**
**目标**：实现中英文切换  
**代码**：  
```python
from PySide6.QtCore import QTranslator
translator = QTranslator()
translator.load("zh_CN.qm")
app.installTranslator(translator)
```
**学习点**：多语言文件生成与加载。

#### **Demo 19：自定义控件**
**目标**：继承`QWidget`扩展功能  
**代码**：  
```python
class CustomButton(QPushButton):
    def __init__(self, text):
        super().__init__(text)
        self.setStyleSheet("background: #FF5722;")
```
**学习点**：控件继承与重写。

#### **Demo 20：打包与部署**
**目标**：生成可执行文件  
**代码**：  
```bash
pyinstaller --windowed --name MyApp main.py
```
**学习点**：`PyInstaller`配置与安装包制作。

