# PySide6 应用程序打包指南

本文档提供了使用 PyInstaller 打包 PySide6 应用程序的详细指南，包括不同操作系统的注意事项和常见问题解决方法。

## 一、准备工作

### 1. 安装必要的工具

```bash
# 安装 PyInstaller
pip install pyinstaller

# 确保 PySide6 已安装
pip install PySide6
```

### 2. 准备应用程序资源

- 将所有资源文件（图片、图标、数据文件等）放在一个专门的文件夹中，如 `resources/`
- 确保代码中使用相对路径访问这些资源文件
- 准备应用图标文件（Windows 需要 .ico 格式，macOS 需要 .icns 格式）

## 二、基本打包命令

### Windows 系统

```bash
# 基本打包（生成目录结构）
pyinstaller --name="MyApp" --windowed 20_packaging.py

# 单文件打包
pyinstaller --name="MyApp" --windowed --onefile 20_packaging.py

# 包含资源文件
pyinstaller --name="MyApp" --windowed --add-data="resources;resources" 20_packaging.py

# 添加图标
pyinstaller --name="MyApp" --windowed --icon=resources/app_icon.ico 20_packaging.py

# 完整打包命令
pyinstaller --name="MyApp" --windowed --onefile --icon=resources/app_icon.ico --add-data="resources;resources" 20_packaging.py
```

### macOS 系统

```bash
# 基本打包（生成 .app 包）
pyinstaller --name="MyApp" --windowed 20_packaging.py

# 单文件打包
pyinstaller --name="MyApp" --windowed --onefile 20_packaging.py

# 包含资源文件（注意分隔符是冒号）
pyinstaller --name="MyApp" --windowed --add-data="resources:resources" 20_packaging.py

# 添加图标
pyinstaller --name="MyApp" --windowed --icon=resources/app_icon.icns 20_packaging.py

# 完整打包命令
pyinstaller --name="MyApp" --windowed --onefile --icon=resources/app_icon.icns --add-data="resources:resources" 20_packaging.py
```

### Linux 系统

```bash
# 基本打包
pyinstaller --name="MyApp" --windowed 20_packaging.py

# 包含资源文件（与 macOS 相同，使用冒号分隔）
pyinstaller --name="MyApp" --windowed --add-data="resources:resources" 20_packaging.py
```

## 三、高级打包配置

### 使用 spec 文件

1. 首先生成基本 spec 文件：
   ```bash
   pyinstaller --name="MyApp" 20_packaging.py
   ```

2. 编辑生成的 `MyApp.spec` 文件，可以参考项目中的 `MyApp.spec.example`

3. 使用修改后的 spec 文件打包：
   ```bash
   pyinstaller MyApp.spec
   ```

### 常用 spec 文件配置项

- `datas`: 添加数据文件
- `binaries`: 添加二进制文件
- `hiddenimports`: 添加隐式导入的模块
- `excludes`: 排除不需要的模块
- `icon`: 设置应用图标
- `console`: 是否显示控制台窗口

## 四、资源文件处理

### 在代码中访问资源文件

```python
def resource_path(relative_path):
    """获取资源的绝对路径，兼容开发环境和打包环境"""
    try:
        # PyInstaller 创建临时文件夹，将路径存储在 _MEIPASS 中
        base_path = sys._MEIPASS
    except Exception:
        # 开发环境下，使用当前目录
        base_path = os.path.abspath(".")
    
    return os.path.join(base_path, relative_path)

# 使用示例
icon_path = resource_path("resources/app_icon.svg")
pixmap = QPixmap(icon_path)
```

## 五、常见问题及解决方法

### 1. 找不到资源文件

- 确保使用了 `resource_path()` 函数获取资源路径
- 检查 `--add-data` 参数格式是否正确（Windows 使用分号，macOS/Linux 使用冒号）
- 确认资源文件路径是否正确

### 2. 缺少依赖模块

- 使用 `--hidden-import=模块名` 添加隐式导入的模块
- 或在 spec 文件中的 `hiddenimports` 列表中添加

### 3. 打包后的应用无法启动

- 尝试使用 `--debug=all` 参数查看详细错误信息
- 检查是否有动态加载的模块或插件未被包含

### 4. 图标问题

- Windows: 确保使用 .ico 格式的图标
- macOS: 确保使用 .icns 格式的图标，或者使用 PNG 图标并在 spec 文件中配置 BUNDLE

## 六、打包优化

### 减小打包体积

- 使用 `--exclude-module=模块名` 排除不需要的模块
- 在 spec 文件中添加 `excludes` 列表
- 使用 UPX 压缩可执行文件（`--upx-dir=UPX目录`）

### 提高启动速度

- 避免使用 `--onefile` 选项，使用目录模式可以提高启动速度
- 减少不必要的导入和依赖

## 七、测试打包应用

在不同的目标环境中测试打包后的应用，确保：

1. 应用能够正常启动
2. 所有功能正常工作
3. 资源文件能够正确加载
4. 没有意外的依赖问题

## 八、发布应用

### Windows

- 创建安装程序（可使用 NSIS、Inno Setup 等工具）
- 或直接分发 zip 压缩包

### macOS

- 创建 DMG 文件
- 或使用 pkgbuild 创建安装包

### Linux

- 创建 deb 或 rpm 包
- 或分发压缩包